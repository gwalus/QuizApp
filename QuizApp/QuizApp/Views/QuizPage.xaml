<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuizApp.Views.QuizPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="HorizontalOptions" Value="Fill"/>
                <Setter Property="VerticalOptions" Value="Fill"/>
                <Setter Property="BackgroundColor" Value="LightSlateGray"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Text="{Binding Answer1}"/>
        <Button Grid.Column="1" Grid.Row="0" Text="{Binding Answer2}"/>
        <Button Grid.Column="0" Grid.Row="1" Text="{Binding Answer3}"/>
        <Button Grid.Column="1" Grid.Row="1" Text="{Binding Answer4}"/>

        <Label Text="{Binding Question}" 
               Grid.Row="2" 
               Grid.ColumnSpan="2" 
               HeightRequest="50"/>
    </Grid>
</ContentPage>